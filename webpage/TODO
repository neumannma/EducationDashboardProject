UI SWITCHING
 * nothing displays when the aspect ratio is exactly 1:1

MOBILE UI
 * add lists
 * animate?

CSS
 * separate custom button class functionality from properties like color (and size?)
 * adopt new naming convention (and flatten out a lot of the nesting)
 * make class for mobile UI round button

JS
 * click event handler for submit button
 * get the map working

GENERAL
 * rename 'filter' to 'candidates'